# 🔄 Updates v0.2.1 - 2026-01-26

## ✨ Новые возможности

### 1️⃣ **Настройка параметров при добавлении индикатора**

Теперь при добавлении индикатора можно сразу настроить диапазоны параметров!

**Что изменилось:**
- ✅ Модальное окно "Add Indicator" расширено
- ✅ Все параметры индикатора отображаются сразу
- ✅ Можно задать Default, Min, Max, Step для каждого параметра
- ✅ Нет необходимости сначала добавлять, а потом редактировать

**Как использовать:**
```
1. Нажмите "+ Add Indicator"
2. Выберите тип индикатора (например, RSI)
3. Настройте Source (Close, Open, High, Low, HL/2, HLC/3, OHLC/4)
4. Для каждого параметра задайте:
   - Default: 14
   - Min: 10
   - Max: 20
   - Step: 1
5. Нажмите "Add Indicator"
```

**Пример настройки RSI:**
```
RSI Indicator
├─ Source: Close
├─ Period
│  ├─ Default: 14
│  ├─ Min: 10
│  ├─ Max: 20
│  └─ Step: 1
├─ Overbought
│  ├─ Default: 70
│  ├─ Min: 60
│  ├─ Max: 90
│  └─ Step: 5
└─ Oversold
   ├─ Default: 30
   ├─ Min: 10
   ├─ Max: 40
   └─ Step: 5
```

---

### 2️⃣ **Поле Source для индикаторов**

Каждый индикатор теперь имеет настройку Source!

**Доступные источники данных:**
- `Close` - цена закрытия (default)
- `Open` - цена открытия
- `High` - максимум
- `Low` - минимум
- `HL/2` - среднее High и Low
- `HLC/3` - среднее High, Low и Close
- `OHLC/4` - среднее Open, High, Low и Close

**Где отображается:**
- В карточке индикатора (badge "Source: Close")
- При добавлении индикатора (dropdown)
- При редактировании индикатора (dropdown)

**Зачем это нужно:**
Разные индикаторы работают лучше с разными источниками:
- RSI обычно использует Close
- Moving Averages могут использовать HL/2 или HLC/3
- Volatility индикаторы часто используют High/Low

---

### 3️⃣ **Унифицированный редактор сигналов**

Формулы BUY и SELL объединены в один блок!

**Старый подход:**
```
BUY Signal Formula:
RSI < 30 AND Close > EMA

SELL Signal Formula:
RSI > 70 OR Close < EMA
```

**Новый подход:**
```
Signal Formulas:
IF RSI < 30 AND Close > EMA THEN BUY
IF RSI > 70 OR Close < EMA THEN SELL
```

**Преимущества:**
- ✅ Более читаемый синтаксис
- ✅ Вся логика в одном месте
- ✅ Поддержка комментариев (#)
- ✅ Гибкие условия (можно комбинировать)
- ✅ Кнопка "Reset" для возврата к шаблону

**Синтаксис:**

```python
# Комментарии начинаются с #
# Это будет проигнорировано

# Базовый синтаксис
IF [condition] THEN [action]

# Примеры условий:
IF RSI < 30 THEN BUY
IF RSI > 70 THEN SELL

# Логические операторы:
IF RSI < 30 AND Close > EMA THEN BUY
IF RSI > 70 OR Close < EMA THEN SELL

# Скобки для группировки:
IF (RSI < 30 AND Close > EMA) OR (BB_lower > Close) THEN BUY

# Множественные условия:
IF RSI < 30 AND Close > EMA AND Volume > 1000000 THEN BUY
IF RSI > 70 THEN SELL
IF Close < BB_lower THEN BUY
```

**Доступные элементы:**
- **Price data**: Close, Open, High, Low, Volume
- **Indicators**: RSI, EMA, MACD, BB, CG_DW (добавленные индикаторы)
- **Parameters**: RSI_period, EMA_period, и т.д.
- **Operators**: <, >, <=, >=, ==, !=
- **Logic**: AND, OR, NOT
- **Actions**: BUY, SELL

---

## 🎨 UI/UX улучшения

### Модальное окно "Add Indicator":
- Увеличено до `max-w-3xl` (было `max-w-lg`)
- Добавлен scroll для длинных списков параметров
- Dropdown для Source
- Визуальные карточки для каждого параметра
- Все параметры настраиваются сразу

### Модальное окно "Edit Indicator":
- Добавлен dropdown для Source
- Source редактируется вместе с параметрами

### Карточка индикатора:
- Badge с Source отображается рядом с названием
- Визуальная иерархия улучшена

### Редактор формул:
- Увеличен до 16 строк (было 8)
- Добавлен footer с подсказками по синтаксису
- Кнопка "Reset" для быстрого возврата к шаблону
- Цветовые подсказки в footer (emerald для синтаксиса, amber для операторов)

---

## 📊 Примеры использования

### Пример 1: RSI с кастомным Source

```
Добавить RSI:
├─ Type: RSI
├─ Source: HL/2  (вместо Close)
└─ Parameters:
   ├─ Period: 14 (min: 10, max: 20, step: 1)
   ├─ Overbought: 75 (min: 70, max: 90, step: 5)
   └─ Oversold: 25 (min: 10, max: 30, step: 5)

Formula:
IF RSI < 25 THEN BUY
IF RSI > 75 THEN SELL
```

### Пример 2: EMA с HLC/3

```
Добавить EMA:
├─ Type: EMA
├─ Source: HLC/3  (среднее High, Low, Close)
└─ Parameters:
   └─ Period: 20 (min: 10, max: 50, step: 5)

Formula:
IF Close > EMA AND Volume > 1000000 THEN BUY
IF Close < EMA THEN SELL
```

### Пример 3: Комплексная стратегия

```
Indicators:
1. RSI (Source: Close, Period: 14)
2. EMA (Source: HLC/3, Period: 20)
3. BB (Source: Close, Period: 20, Std: 2)

Formula:
# Entry conditions
IF RSI < 30 AND Close > EMA AND Close < BB_lower THEN BUY

# Exit conditions
IF RSI > 70 THEN SELL
IF Close < EMA THEN SELL

# Stop-loss
IF Close < BB_lower * 0.95 THEN SELL
```

---

## 🔧 Технические детали

### Новые константы:
```javascript
const SOURCE_OPTIONS = ["Close", "Open", "High", "Low", "HL/2", "HLC/3", "OHLC/4"];
```

### Обновленная структура индикатора:
```javascript
{
  id: 123,
  type: "RSI",
  name: "RSI (Relative Strength Index)",
  source: "Close",  // НОВОЕ ПОЛЕ
  custom: false,
  params: [
    { key: "period", label: "Period", default: 14, min: 10, max: 20, step: 1 }
  ],
  metrics: ["Overbought/Oversold", "Divergence Quality"]
}
```

---

## 📝 Migration Guide

Если у вас были индикаторы из предыдущей версии:

**До v0.2.1:**
```javascript
// Индикаторы НЕ имели поля source
indicator = { id, type, name, params, metrics }
```

**После v0.2.1:**
```javascript
// Все индикаторы теперь ИМЕЮТ поле source
indicator = { id, type, name, source, params, metrics }
```

**Рекомендация:** Пересоздайте индикаторы через новый UI, чтобы задать Source.

---

## ⚠️ Breaking Changes

1. **FormulaEditor**: Теперь принимает один prop `value` вместо разделения на BUY/SELL
2. **Indicator structure**: Добавлено обязательное поле `source`
3. **Синтаксис формул**: Теперь используется `IF ... THEN ...` вместо отдельных блоков

---

## 🚀 Следующие шаги

После обновления:

1. ✅ Откройте приложение: http://localhost:3000
2. ✅ Добавьте новый индикатор и настройте Source
3. ✅ Попробуйте новый синтаксис формул
4. ✅ Изучите примеры выше
5. 🎯 Готовьтесь к Stage 2-5!

---

## 📞 Changelog

**Version:** 0.2.1  
**Release Date:** 2026-01-26  
**Author:** bogdan  

**Changes:**
- Added Source field to indicators
- Unified BUY/SELL formulas into single "Signals" block
- Parameter ranges now configurable during indicator creation
- Improved modal layouts
- Enhanced syntax with IF-THEN structure
- Added reset button to formula editor
- Expanded modal sizes for better UX

**Bug Fixes:**
- Fixed indicator parameter initialization
- Improved state management for modal forms

---

**Приятного использования! 🎉**
